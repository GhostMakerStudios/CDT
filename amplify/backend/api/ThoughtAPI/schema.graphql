type Thought
  @model
  @auth(rules: [{ allow: public, operations: [read, create, update, delete] }]) {
  id: ID!
  date: AWSDate!
  title: String!
  message: String!
  tags: [String!]!
  embedding: [Float!]!
  audioUrl: String
}
